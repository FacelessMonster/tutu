
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <%=link_to search_path do %>
      <img class="nav-brand" src="/tutu1.png" width="64" height="32" alt="">
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if user_signed_in? && current_user.admin? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= admin_trains_path %>">Trains</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= admin_routes_path %>">Routes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= admin_railway_stations_path %>">Stations</a>
        </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link" href="<%= search_path %>">Ticket search</a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% unless signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_session_path %>">Log in</a>
        </li>
        <% else %>
        <li class="nav-item">
          <a class="nav-link" href="<%= tickets_path %>">My tickets</a>
        </li>
        <li class="nav-item">
          <%= link_to "Quit", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
        <% end %>
      </ul>
    </div>
  </nav>
