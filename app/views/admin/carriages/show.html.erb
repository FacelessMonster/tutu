
      <div class="card text-white bg-dark mb-3" style="max-width: 18rem;">
        <div class="card-header">Поезд: <%= @carriage.train.number %> Вагон № <%= @carriage.number%>.</div>
        <div class="card-body">
          <h5 class="card-title">Тип вагона: <%= @carriage.type%>.</h5>
          <p class="card-text">Количество мест: <%= @carriage.seats%>.</p>
        </div>
      </div>
      <%= link_to "Back", admin_train_path(@carriage.train) %>
